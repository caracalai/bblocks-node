const { Camera } = require('./Camera');
const { Image } = require('./Image');
const { Serializer } = require('./Serializer');
const { Tuple } = require('./Tuple');

module.exports = {
  Image, Camera, Serializer, Tuple,
};
